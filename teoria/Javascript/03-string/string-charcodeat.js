const sentence = "The quick brown fox jumps over the lazy dog.";

const index = 2;

console.log(`The character code  ${sentence.charCodeAt(index)}`);

// Curiosidad

const icon = '⚡️';

console.log(`The character point  ${icon.codePointAt(1)}`);